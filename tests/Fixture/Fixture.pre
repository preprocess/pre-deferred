<?php

namespace Pre\Deferred\Fixture;

class Fixture
{
    public function touch()
    {
        $path = __DIR__ . "/touched";

        defer {
            unlink($path);
        };

        touch($path);

        return file_exists($path);
    }
}
